/** @type {import('next').NextConfig} */
const nextConfig = {
    // images 
    images: {
        domains: ['img.freepik.com', 'icons8.com', 'cdn0.iconfinder.com'],
    },
}

module.exports = nextConfig
